<section class="subscribe">
  <div class="subscribe-inner">
    <h2 class="subscribe-heading">
      Subscribe <em>to</em> Aster
    </h2>
    <p class="subscribe-text">
      From a language teacher who never stopped being a learner
    </p>
    <div id="audienceful-5qfwcu"></div>
  </div>
</section>

<!-- Audienceful.js -->
<script is:inline data-orgid="org_Zaaub6fq" src="https://cdn.audiencefulapp.net/forms/audienceful.min.js"></script>
<!-- End Audienceful.js -->

<style>
  .subscribe {
    background: var(--color-black);
    border-radius: var(--radius);
    padding: 62px 60px;
    text-align: center;
    margin: 60px 0;
  }

  .subscribe-inner {
    max-width: 600px;
    margin: 0 auto;
  }

  .subscribe-heading {
    color: #ffffff;
    font-size: 40px;
    font-weight: 700;
    margin-bottom: 16px;
  }

  .subscribe-heading em {
    font-family: var(--font-accent);
    font-style: italic;
    font-weight: 400;
  }

  .subscribe-text {
    color: rgba(255, 255, 255, 0.6);
    font-size: 16px;
    margin-bottom: 30px;
  }

  @media (max-width: 768px) {
    .subscribe {
      padding: 40px 24px;
    }

    .subscribe-heading {
      font-size: 28px;
    }
  }
</style>
